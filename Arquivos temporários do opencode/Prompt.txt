Quero que você continue o desenvolvimento do J.A.R.V.I.S. neste workspace a partir do código REAL que já existe, sem recriar o projeto do zero e sem apagar ou substituir componentes corretos; antes de modificar qualquer coisa, analise todo o repositório atual, leia AGENTS.md, README.md, toda a documentação, core/contracts, core/planner, memory, security, server, tools, windows_agent, tests, pyproject.toml, opencode.json e todos os arquivos relevantes, identifique exatamente o que já está implementado e então IMPLEMENTE O QUE AINDA FALTA; o projeto já possui uma fundação com contratos, ToolRegistry, PolicyEngine, ferramentas básicas, memória SQLite, planner/ExecutionPlan, FastAPI e Windows Agent, portanto NÃO recrie essas partes sem necessidade e preserve suas interfaces quando estiverem corretas; o objetivo agora é transformar essa fundação em um J.A.R.V.I.S. funcional e orientado a objetivos, capaz de receber uma mensagem de texto ou futuramente voz, entender a intenção usando um LLM, consultar memória e estado do sistema, decidir se deve responder diretamente ou usar uma ferramenta, executar ferramentas reais de forma segura, criar e acompanhar Tasks de múltiplas etapas, verificar os resultados e continuar automaticamente até que o objetivo esteja concluído, bloqueado, cancelado ou aguardando uma decisão do usuário; implemente primeiro uma camada LLM Provider abstrata, desacoplada do modelo específico, com uma interface clara para geração de texto, streaming e tool calling estruturado, deixando pronta uma implementação Ollama para o futuro i5 mas sem exigir que o Pentium execute um modelo pesado agora; o código deve permitir configurar posteriormente algo como Ollama no i5 sem reescrever o restante do sistema; implemente Conversation Manager e Conversation API para receber mensagens, manter conversation_id, buscar memória relevante, construir contexto e encaminhar a solicitação ao Orchestrator; crie endpoints REST claros para conversation, tasks, status, devices, tools, memory, events e health e adicione WebSocket ou mecanismo equivalente para streaming de respostas, progresso de Tasks, chamadas de ferramentas e eventos em tempo real; implemente um Orchestrator central que seja responsável por receber a intenção do usuário, consultar memória quando necessário, consultar estado do sistema quando necessário, verificar capacidades disponíveis, decidir entre resposta direta, chamada de ferramenta ou criação de Task, chamar o LLM com contexto e ferramentas adequadas e processar tool calls retornados pelo modelo; o LLM nunca deve executar comandos diretamente e nunca deve ser a fonte da verdade sobre CPU, RAM, disco, processos, arquivos, bateria, estado dos PCs, aplicativos ou outros dados reais; toda informação do mundo real deve vir de ferramentas; conecte o Orchestrator ao ToolRegistry que já existe e use o contrato de BaseTool/ToolMetadata/ToolResult já existente em vez de criar um sistema paralelo; implemente adaptação das ferramentas existentes para tool calling estruturado, convertendo ToolMetadata em schemas que o LLM consiga utilizar; o fluxo deve ser LLM -> tool call estruturado -> ToolRegistry -> PolicyEngine -> Tool -> resultado real -> ToolResult -> Orchestrator -> LLM; implemente validação de argumentos antes da execução e não permita tool calls inexistentes; nunca criar endpoint genérico de shell, CMD ou PowerShell como /execute?cmd=qualquer_coisa; mantenha o modelo limitado às ferramentas explicitamente registradas e autorizadas; melhore a arquitetura de segurança existente sem removê-la, usando GREEN/YELLOW/RED já definidos no projeto, e faça o PermissionEngine ser realmente consultado antes de cada execução; GREEN pode executar automaticamente, YELLOW deve gerar WAITING_CONFIRMATION até o usuário aprovar, RED exige confirmação forte e explícita; operações destrutivas, gastos, compras, publicação de anúncios, alteração de campanhas, envio em massa, exclusão, alteração de firewall, alteração de credenciais ou execução de código arbitrário nunca podem ocorrer sem confirmação explícita; implemente Confirmation Manager para que uma confirmação possa futuramente vir pela UI, voz ou Home Assistant, mas permita testar tudo localmente agora; implemente Task Manager persistente usando a memória SQLite existente, com task_id, objective, context, priority, status, steps, current_step, progress, timestamps, results, errors, retry_count e waiting_reason, utilizando estados CREATED, PLANNING, RUNNING, WAITING_CONFIRMATION, WAITING_USER, WAITING_RESOURCE, PAUSED, VERIFYING, COMPLETED, FAILED e CANCELLED; faça o Task Manager recuperar Tasks após reinicialização e permitir consultar status, pausar, retomar e cancelar; integre o Planner existente ao Task Manager para que objetivos complexos sejam transformados em ExecutionPlan usando os contratos já presentes; não substitua o PlanBuilder atual sem necessidade; crie o fluxo Planner -> Task Steps -> Executor; implemente Executor real que execute cada etapa, aplique política, tenha timeout, retries limitados, cancelamento, logs, request_id e task_id e retorne resultados estruturados; implemente Verification Engine separado do Executor, pois executar uma ferramenta não significa que a ação teve sucesso; cada etapa importante deve possuir uma estratégia de verificação; exemplos: abrir aplicativo deve verificar se o processo/janela existe, ligar Core deve verificar ping e health check, iniciar serviço deve consultar sua API, criar arquivo deve verificar sua existência, publicar algo deve verificar o estado real da plataforma; faça a Task só ficar COMPLETED quando o objetivo tiver sido efetivamente verificado; implemente replanejamento limitado quando uma etapa falhar, sem loops infinitos; se uma ação falhar o sistema deve registrar a falha, tentar alternativa somente se for segura e permitida e, se necessário, entrar em WAITING_USER ou FAILED; implemente Global System State e Capability Registry para SERVER, CORE, MOBILE, PANEL e NODE2, distinguindo OFFLINE, STARTING, ONLINE, READY, BUSY, DEGRADED e ERROR; diferencie ONLINE de READY, pois o i5 pode estar ligado mas ainda não ter Windows Agent ou Ollama disponíveis; implemente heartbeat e last_seen; crie Device Registry usando as estruturas existentes quando possível; cada dispositivo deve informar device_id, type, status, capabilities, version e last_seen; implemente Capability Router para escolher recursos com base em capabilities e disponibilidade, por exemplo LLM/vision/STT/TTS no i5, automação Windows no Windows Agent, Android no S20, Wake-on-LAN e infraestrutura no i3 e painel no Tab E; implemente API para registrar dispositivos e atualizar heartbeats; melhore a memória existente para suportar não apenas armazenamento, mas recuperação de contexto; crie Memory Service sobre a implementação SQLite existente para preferences, device state, conversation summaries, task state, events e memória operacional; implemente memory.search e memory.save com política de memória e não salve tudo indiscriminadamente; crie Conversation Context Builder que combine mensagem atual, memória relevante, estado do sistema, Task atual, resultados recentes de ferramentas, ferramentas autorizadas e instruções de segurança; não exponha chain-of-thought interno ao usuário, mostrando somente resumo seguro de progresso; implemente suporte a mensagens do tipo "o que você está fazendo?" respondendo com o estado atual da Task sem expor raciocínio privado; implemente separação entre CONVERSATION e TASK, de modo que uma Task possa continuar executando mesmo depois de uma resposta ser enviada; o usuário deve poder perguntar pelo progresso, pausar ou cancelar a Task sem destruir a conversa; implemente eventos como TASK_CREATED, TASK_STARTED, TOOL_CALL, TOOL_RESULT, STEP_COMPLETED, WAITING_CONFIRMATION, WAITING_USER, TASK_COMPLETED e TASK_FAILED; envie esses eventos por WebSocket para a UI; crie uma UI web separada do backend, sem lógica de negócio dentro da interface, consumindo apenas as APIs do JARVIS; a UI deve permitir chat/texto, visualizar resposta, visualizar Tasks, progresso, dispositivos, ferramentas permitidas, memória relevante quando apropriado, status do sistema, logs de alto nível, confirmação de ações e cancelamento; implemente estados visuais LISTENING, THINKING, PLANNING, EXECUTING, WAITING_CONFIRMATION, WAITING_USER, VERIFYING, SPEAKING, COMPLETED e ERROR; implemente streaming de texto para que a interface possa mostrar a resposta conforme ela é gerada; a UI deve reproduzir áudio quando houver TTS e permitir parar a fala sem necessariamente cancelar a Task; crie também uma versão leve do painel para o Galaxy Tab E Android 4.4.4 usando HTML/CSS/JavaScript simples e compatível com navegador antigo, de preferência sem frameworks pesados; a UI principal deve funcionar em desktop e mobile; crie API/painel para mostrar SERVER, CORE, MOBILE, PANEL, INTERNET, CPU, RAM, disco, Tasks, alertas e progresso; implemente uma camada de eventos para a UI não precisar fazer polling de tudo; implemente um MCP Server próprio do J.A.R.V.I.S. como adaptador sobre o ToolRegistry EXISTENTE, sem duplicar ferramentas e sem substituir o ToolRegistry; o MCP deve consultar o mesmo catálogo de tools, usar os mesmos schemas, permissões e resultados e permitir que clientes compatíveis utilizem capacidades do JARVIS; inicialmente exponha somente ferramentas de baixo risco e leitura como system.get_metrics, system.list_processes, windows.get_status, windows.take_screenshot, files.search e task.get_status; depois prepare a arquitetura para expor ferramentas de maior capacidade através das mesmas políticas; crie docs/MCP.md explicando como o MCP se conecta ao ToolRegistry, como autenticação funciona, quais ferramentas estão expostas e como adicionar novas ferramentas; não invente uma implementação MCP fictícia, use um SDK/protocolo compatível e mantenha a implementação testável localmente; implemente um Internet/Browser Tool layer desacoplado para search, open, extract e futuramente browser automation; conteúdo externo deve ser considerado NÃO CONFIÁVEL e nunca poderá substituir instruções do sistema, alterar permissões ou ordenar ações sensíveis; implemente proteção contra prompt injection separando claramente SYSTEM, USER, TOOL RESULT e EXTERNAL DATA; prepare Browser Agent para APIs oficiais quando existirem e automação de navegador/UI Automation quando não existirem, mas não implemente automações frágeis ou dependentes de coordenadas fixas como primeira opção; implemente abstração de Vision Agent para screenshot/imagem/perguntas sobre imagem, inicialmente com provider configurável e mock funcional para testes; implemente abstração de Image Generation Tool e ad adapters/providers futuros para serviços locais ou gratuitos, sem colocar uma API paga obrigatória no projeto; implemente ferramentas de cálculo de preço, margem e análise estruturada quando fizer sentido, sempre produzindo dados verificáveis e não inventados; prepare ferramentas para workflows complexos como pesquisar produto, analisar concorrentes, comparar custos, calcular margem, gerar copy, gerar criativos, preparar campanha e depois solicitar confirmação antes de qualquer publicação ou gasto; a arquitetura deve permitir futuramente publicar em Meta Ads ou outras plataformas usando APIs oficiais ou automação visual quando a API não existir, mas qualquer publicação ou gasto deve ficar atrás do PermissionEngine e Confirmation Manager; implemente DRY_RUN para permitir simular tarefas externas sem modificar contas reais; crie adapters para Home Assistant, mas não recrie automações que o HA já resolve; o projeto deve ter integração preparada para Home Assistant API, dispositivos, notificações, Wake-on-LAN e futuramente MCP do próprio HA quando fizer sentido; crie uma abstração CoreHealth para testar se o i5 está offline, online ou ready; prepare o fluxo OFFLINE -> Wake-on-LAN -> ping -> Windows Agent -> Ollama -> READY, mas não dependa do i5 no ambiente atual; mantenha o servidor útil sem Core, permitindo Tasks que não exigem LLM pesado e marcando corretamente tarefas que precisam do Core; implemente uma Voice Architecture desacoplada, com STT, Orchestrator, TTS e Wake Word como módulos substituíveis; não instale serviços pesados no Pentium neste momento; crie interfaces para Whisper/faster-whisper, Piper e wake word, com mocks para teste e adapters preparados para o i5; a interface de voz futura deverá enviar device_id e capabilities junto da mensagem para o servidor, permitindo saber se a entrada veio do S20, do PC, do Tab E ou de outro cliente; o servidor nunca deve adivinhar o dispositivo de origem; cada cliente deve registrar device_id, type, capabilities e session_id; quando uma entrada vier do S20 com camera, microphone e speaker disponíveis, o Orchestrator poderá escolher ferramentas Android apropriadas; quando vier do desktop, poderá escolher ferramentas do Windows Agent; quando vier do Tab E, tratar como painel; prepare Android Agent baseado inicialmente em Home Assistant Companion e ADB, sem criar app nativo desnecessário agora; crie adapters futuros para bateria, sensores, notificações, screenshot, abrir aplicativos e outras capacidades; crie sistema de streaming e routing de áudio para que uma resposta textual possa ser convertida em TTS e reproduzida no mesmo dispositivo que iniciou a conversa; se no futuro houver Google Home, trate-o apenas como mais um terminal/integração, nunca como cérebro central; documente a arquitetura de voz, identificação de dispositivo e retorno da resposta; crie testes completos para LLM provider usando mocks, tool calling, planner, task lifecycle, executor, verification, permissions, confirmation, memory, API, WebSocket, MCP, prompt injection, path traversal, command injection, invalid tool call, unknown tool, offline Core, failed tool, retries e task recovery; execute pytest e todos os testes disponíveis; corrija todos os erros encontrados; execute o servidor localmente e valide endpoints; valide a UI; valide MCP em modo local/mock; valide pelo menos uma Task multi-etapas completa em mock, incluindo uma etapa que chama ferramenta, uma etapa de verificação e uma etapa de conclusão; não marque integração externa como funcional se ela não puder ser testada neste ambiente; crie Mocks claramente separados de Production Providers; atualize README.md e toda a documentação para refletir o estado real do projeto; crie docs/ARCHITECTURE.md, docs/LLM.md, docs/ORCHESTRATOR.md, docs/TASKS.md, docs/MCP.md, docs/UI.md, docs/VOICE.md, docs/INTERNET.md, docs/ANDROID.md, docs/HOME_ASSISTANT.md, docs/SECURITY.md e docs/DEPLOYMENT.md se ainda não existirem, mantendo a terminologia atual do projeto; não introduza Redis, PostgreSQL, ChromaDB, RabbitMQ, Kubernetes ou outros componentes pesados sem necessidade concreta; não instale modelos grandes no Pentium; não adicione dependências desnecessárias; preserve o projeto portátil para o futuro i5 e i3; qualquer segredo deve ficar fora do código e do Git; revise .gitignore; ao final execute uma auditoria completa do que foi construído, mostre a árvore do projeto, liste arquivos novos/modificados, testes executados, testes aprovados, integrações funcionais em mock, integrações aguardando i5, i3, S20, Home Assistant ou credenciais externas, comandos de execução local, comandos de teste e próximos passos; NÃO pare apenas na análise e NÃO responda apenas com um plano: implemente diretamente no workspace o que for possível agora, reaproveitando e aprimorando o código existente, executando testes e corrigindo problemas antes de considerar o trabalho concluído; somente peça minha intervenção quando realmente existir uma dependência externa, credencial ou decisão irreversível que não possa ser simulada com segurança.
